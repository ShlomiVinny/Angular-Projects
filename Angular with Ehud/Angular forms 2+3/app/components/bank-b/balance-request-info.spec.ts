import { BalanceRequestInfo } from './balance-request-info';

describe('BalanceRequestInfo', () => {
  it('should create an instance', () => {
    expect(new BalanceRequestInfo()).toBeTruthy();
  });
});

import { MtSecureDirective } from './mt-secure.directive';

describe('MtSecureDirective', () => {
  it('should create an instance', () => {
    const directive = new MtSecureDirective();
    expect(directive).toBeTruthy();
  });
});

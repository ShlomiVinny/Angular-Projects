import { ContainerStyle.Model } from './container-style.model';

describe('ContainerStyle.Model', () => {
  it('should create an instance', () => {
    expect(new ContainerStyle.Model()).toBeTruthy();
  });
});
